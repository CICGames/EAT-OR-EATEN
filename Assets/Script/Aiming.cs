using UnityEngine;
using System.Collections;

public class Aiming : MonoBehaviour {

    float lookFactor = 0.8f;

    // Use this for initialization
    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {/*
        Vector3 cc = Input.mousePosition;
        cc.z = 4f;
        Vector3 mouseWorld = PlayerController.playerCamera.GetComponent<Camera>().ScreenToWorldPoint(cc);
        mouseWorld.y = 0.3f;

        Debug.Log(mouseWorld);
        transform.LookAt(mouseWorld);*/
    }
}
